{% extends 'base.html.twig' %}

{% block title %}Hello AddItemController!
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_label(form.title) }}
        {{ form_widget(form.title) }}
   
        {{ form_label(form.description) }}
        {{ form_widget(form.description) }}

        {{ form_rest(form) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}